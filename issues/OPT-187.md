---
id: OPT-187
title: Remove dead orders/order_positions tables and related code
status: Done
priority: Low
assignee: Steve Johnson
created: 2026-03-10
started: 2026-03-10
completed: 2026-03-10
labels: [Improvement]
related: []
linear_url: https://linear.app/optionedge/issue/OPT-187/remove-dead-ordersorder-positions-tables-and-related-code
---

# OPT-187: Remove dead orders/order_positions tables and related code

## Overview

The `orders`, `order_positions`, and `order_chain_members` tables are no longer populated by the current sync pipeline. They are remnants of the old order-based system that was replaced by the raw_transactions → lots → groups pipeline.

## Evidence

* The sync router (`src/routers/sync.py`) never calls `OrderManager.save_order_to_database()` or `process_transactions_to_orders_and_chains()`
* All three tables are empty in production PostgreSQL — confirmed after a fresh user onboarding + full data ingestion ([test-3@example.com](<mailto:test-3@example.com>))
* `order_chain_members` appeared active in code (`update_chain_cache()`) but produces no rows in practice
* The only live `order_manager` usage in routers is read-only (`GET /api/order/{id}`) and a one-time migration endpoint (`POST /api/migrate-realized-pnl`)

## What to remove

### Tables

* `orders` — never written to by current pipeline
* `order_positions` — never written to by current pipeline
* `order_chain_members` — code paths exist but produce no rows; confirmed empty after fresh sync

### Code

* `OrderManager.save_order_to_database()` and related write methods in `src/models/order_models.py`
* `OrderManager.process_transactions_to_orders_and_chains()`
* `OrderManager.reprocess_orders_and_chains_from_database()`
* ORM models `Order`, `OrderPosition`, `OrderChainMember` in `src/database/models.py`
* References in `src/routers/sync.py` initial sync cleanup (line 254)
* `OrderChainMember` usage in `src/services/chain_service.py` (delete/insert calls that produce nothing)
* Dead imports across `dependencies.py`, `chain_service.py`, etc.
* Associated test files (`tests/unit/test_strategy_detector.py` if only testing old path)
* Various investigation scripts in project root (`investigate_*.py`, `confirm_zebra_pattern.py`, etc.)

### Keep

* `order_chains` and `order_chain_cache` — still active
* `OrderManager` read methods if still used by `/api/order/{id}` endpoint (evaluate if that endpoint is still needed)

## Migration

* Add Alembic migration to drop `orders`, `order_positions`, and `order_chain_members` tables
* Clean up FK references before dropping
