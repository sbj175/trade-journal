---
id: OPT-200
title: Group-level roll chain tracking via rolled_from_group_id
status: Done
priority: High
assignee: Steve Johnson
created: 2026-03-12
started: 2026-03-13
completed: 2026-03-13
labels: [Feature]
related: []
linear_url: https://linear.app/optionedge/issue/OPT-200/group-level-roll-chain-tracking-via-rolled-from-group-id
---

# OPT-200: Group-level roll chain tracking via rolled_from_group_id

## Summary

Track roll chains at the **group level** using a new `rolled_from_group_id` column on `PositionGroup`. This replaces OPT-201's lot-level approach — rolls operate at the strategy/group level, not individual lots.

## Changes

1. Schema: Add `rolled_from_group_id` FK on `PositionGroup` + migration
2. Pipeline: Auto-detect roll links in `GroupPersister.process_groups()` Phase 4b
3. API: New `GET /api/ledger/group-roll-chain/{group_id}` endpoint
4. API: Add `rolled_from_group_id` + `has_roll_chain` to `/api/ledger` response

OPT-201 reverted and cancelled — lot-level linking was the wrong abstraction for rolls.

## Comments

### 2026-03-13 — Steve Johnson

Implementation complete. 18 roll links auto-detected across the dataset.

Key insight: group-level roll detection correctly handles strategies like Iron Condors that *cannot* be rolled in a single order (8 legs needed, broker max is 4). The user splits into two orders but their intent is one roll — matching on (strategy_label, same-day close→open) at the group level captures this intent even though lot-level linking never could.

---

### 2026-03-13 — Steve Johnson

## Frontend Implementation Complete

**Branch:** `opt-200-roll-chain-ui`

### Changes

**Removed Orders view (dead after OPT-203):**
- Removed Positions/Orders toggle from group headers
- Removed entire Action View template (~110 lines of template)
- Removed related JS functions: `calculateCreditDebit`, `formatCreditDebit`, `getCreditDebitDivisor`, `getOrderComment`, `updateOrderComment`, `sortPositions`, `formatAction`, `getDisplayQuantity`
- Removed `viewMode`, `orderComments` state
- Backend: removed `OrderChainCache` query and `orders` from `/api/ledger` response (-28 lines)

**Added Roll Chain UI:**
- Link icon (`fa-link`) on group header rows when `has_roll_chain` is true
- Clicking icon fetches `/api/ledger/group-roll-chain/{group_id}` and shows timeline panel
- Panel shows all groups in chain with opening/closing dates, strategy, status, realized P&L
- Current group highlighted with blue background
- Cumulative P&L shown in panel header
- Clicking a different group scrolls to and expands it on the page

**Added "Rolls" filter chip:**
- New filter button in the filter bar (between Status and Strategy)
- Filters to only groups with `has_roll_chain === true`

**Build verified:** `npm run build` succeeds with no errors.
